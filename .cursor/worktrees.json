{
  "setup-worktree-unix": [
    "python -m venv venv",
    "source venv/bin/activate && pip install -r requirements.txt && pip install -e .",
    "cd eeg_pipeline/cli/tui && go mod download"
  ],
  "setup-worktree-windows": [
    "python -m venv venv",
    ".\\venv\\Scripts\\Activate.ps1; pip install -r requirements.txt; pip install -e .",
    "cd eeg_pipeline\\cli\\tui; go mod download"
  ],
  "setup-worktree": [
    "pip install -r requirements.txt",
    "pip install -e ."
  ]
}
